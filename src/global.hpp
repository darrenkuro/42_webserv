#pragma once

#include <set>
#include <map>
#include <vector>
#include <iostream>

using std::cout;
using std::cerr;
using std::endl;
using std::vector;
using std::map;
using std::set;
using std::exception;
using std::runtime_error;

typedef std::vector Vector;
typedef struct pollfd PollFd;